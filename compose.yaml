services:
  latex:
    # สภาพแวดล้อมคอมไพล์ที่เหมือนกันทุกเครื่อง / Reproducible build environment
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /workspace
    volumes:
      - .:/workspace
